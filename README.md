# extra-serial-uart-pin-for-arduino
low-cost TX UART PIN for your system if you don't have extra UART pin. This algorithm and method can implemented on Raspberry or STM32 board if you need extra TX pin.
